formally 规定一下：取指不强制要求三周期，访存指令需要三周期的模拟 .



0. 建议按硬件模块思考&组织代码，而非按stage。建议先画架构图。
1. 如果你要模拟硬件的wire，可以使用const函数的形式，比如 rob.is_full()
2. 谨慎使用所有有输入参数/非const的函数，且他们都必须要有每周期调用上限，因为他们会对应硬件的port,而read/write port数目是固定且很有限的。优先考虑跨周期完成。
3. 禁止直接读下周期的状态，如果你需要，可以想想如何用这周期的状态+输入(如果你有wire input/port的话)来predict下周期
4. 不要让我看到while循环。你每周期只需要issue/execute/cdb broadcast/commit一条指令。其他循环请牢记有多少次循环就有多少份相应的元件，想想这玩意会要多少代价再写
5. 你的代码应当能够交换执行顺序，如果你说wire input,请参考 1/bonus1.

新增bonus:
1.使用传奇学长darksharpness的模板: https://github.com/DarkSharpness/RISC-V-Simulator-Template/。 理论上如果你的代码是硬件风格的，改起来应该比较简单。不使用模板但展示你自己构思的相似抽象，并能够阐述设计应该如何对应硬件也可酌情给分。






各 module 的执行顺序可以任意交换 的要求是交换顺序不仅仍然能够给出正确结果，且行为完全一致（检查打乱后的周期数是否和打乱前一致）。如果你的程序遵守了硬件逻辑的话，这应当是可以保证的




and 注意尽量避免：1.读next 2.（跨module）多次写下周期的值